# MaxHook 协议栈 — fold 算术数据流实测确认（第 248 轮）

日期：2026-08-14
范围：纯离线（Unicorn 具体执行 + fold 算术插桩）

## 一、fold 算术数据流实测（fold_arith_trace，77 条）

第 248 轮在 harness 增加 fold 算术插桩（11 个真实算术 op 的 16 寄存器捕获），实测 fold 执行：

**r13-chain**（inst 230929-230934）：
```
0x180c27743 sub r13d, ebp   : r13 = 0xd8a99313
0x180c27747 add r13d, 0x47f75fb8 : r13 = 0x69ff0917
0x180c2774e not r13d        : r13 = 0xb1f668cf
0x180c27751 neg r13d        : r13 = 0x4e099730
0x180c27754 not r13d        : r13 = 0xb1f668d0
```

**rdx-chain**（inst 231137-231147）：
```
0x180c27936 shl rdx, 1      : rdx = 0xffffffffffdf79ff
0x180c276c5 shr rdx, 3      : rdx = 0xffffffffffbef3fe
0x180c2769b add rdx, rbx    : rdx = 0x200000007eef6cfb, rbx = 0xe000000000082182
0x180c2769e sub rdx, 0x7ef78e7d : rdx = 0x7ef78e7d
```

## 二、关键结论

1. **fold 算术确定性确认**：inst 230929 与 inst 1231470 的寄存器值**完全一致**（跨执行确定性）。
2. **fold 算术正确执行**：r13-chain 的 not/neg/not 与 rdx-chain 的 shl/shr/add/sub 正确执行。
3. **输入是垃圾**：rbx=0xe000000000082182、r13=0xd8a99313 是**指针/堆地址**（非 key-schedule
   状态字），因 VM 数据栈 desync 导致 6 个 fold 输入槽未正确填充。

## 三、最终确认

fold 算术（12 操作 + 8 常量）**正确且确定性**，唯一剩余是 **6 个 fold 输入槽的正确填充**
（key-schedule 状态），受 VM 数据栈 desync 阻断。

### 三·一 第 251 轮校正：fold 算术链是分离的，最终 EDX 来自 VM 数据栈

实测完整 fold 执行（inst 230929-231186）发现：

- **r13-chain**、**rdx-chain**、**aux（r14/r10/r8）** 是**分离的计算**，非收敛到单一 EDX；
- **最终 EDX = `0x2840` 全程不变**，在 `0x180c27be2: mov rdx, [rsp]` 从**VM 数据栈顶**加载；
- 即 fold 的完整流程：6 push → ARX 处理（分离链）→ 结果压栈 → `mov rdx,[rsp]` 加载 EDX → store32。

**关键**：最终 keystream 字来自 VM 数据栈顶（6 push 经 ARX 处理后的结果），非算术链直接收敛。
`0x2840` 是垃圾值（6 push 是 desync 的垃圾），故输出垃圾。

## 四、交付物

本报告 + `encrypt_vm_fold_arith.json`（fold 算术 77 条实测）+ 修改的 harness（fold 算术插桩）。
