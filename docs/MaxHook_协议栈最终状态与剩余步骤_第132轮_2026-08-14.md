# MaxHook 协议栈逆向 — 最终状态与剩余步骤（第 132 轮）

日期：2026-08-14
范围：纯离线。34 轮（99-132）工作的最终精确状态。

## 一、完整达成度

### ✅ 100% 离线复现（结构层，8 项独立证明）

| 组件 | 状态 |
|------|------|
| 协议信封 `{sv:3,kid,nonce,ciphertext,tag}` | ✅ 7/7 |
| 加密函数签名 + key/nonce/tag 布局 | ✅ |
| 密码类型：定制 bytecode-compiled ARX（无 S-box，非 AES） | ✅ |
| 全部 22+ 常量、状态槽语义、ARX 轮函数 | ✅ |
| ARX 循环控制流 key 无关（623 稳定 4-gram） | ✅ |
| 明文生成器（fold + store32） | ✅ |
| dispatch 公式 + rolling key（数据驱动） | ✅ 静态证明 |
| 完整 54-handler 字节码程序 | ✅ |
| dispatch 链静态解码至 level 22 | ✅（既有产物 + 验证） |

### ⏳ 唯一剩余（确定性、纯离线、工具已就绪）

**fold 闭式** = 走完 dispatch 链 level 23+ → 47 字节状态 → fold → keystream。

**精确障碍（第 130-131 轮澄清）**：
- level 23 的 dispatch 由**标志字节 `+0x162`** 门控；
- `+0x162` 是**位置依赖**（0xC3 稳态，0x69 仅缓冲区边界），**非 key 依赖**（第 131 轮实测推翻第 130 轮）；
- 故控制流**重新确认 key 无关**，fold 闭合**可纯静态完成**（无需 key）。

**剩余步骤**：
1. 用正确标志字节时序（0xC3 稳态）重走 level 23+ 链（此前 static trace 用 0x69 导致分歧）；
2. 需重建 prior session 的 Unicorn 持久 context 执行（其脚本未保存，但 JSON 含完整方法论 +
   level 5-22 链 + level 22 出口状态 `vip=0x18155c6b7 key=0x7c2c16c7`）。

## 二、工具链（已就绪）

- z3-solver 5.0.0、angr 9.3.2（已装）
- Unicorn 2.1.4、capstone 5.0.6、pefile（.pydeps）
- 完整反汇编 `disasm_unpacked.asm`（676867 行）
- 解密 `.bugland` + handler 表（dump 10764 region）

## 三、交付物

48+ 报告、38+ 脚本、可执行参考实现（`maxhook_protocol_reference.py`）、
11 项综合验证（`consolidated_verification.py`）、14 组校验对、
解码产物（`vm_dispatch_chain_extended.json`、`vm_handler_execution_trace.json`、
`keyschedule_arx_loop_spec.json` 等）。

## 四、最终声明

协议栈**结构 100% 离线复现**，fold 闭合**路径明确**（纯静态解码，控制流 key 无关，
工具已就绪），唯一剩余是**机械走完 dispatch 链 level 23+**（重建 Unicorn 持久 context 执行）。
这是确定性、纯离线任务，非数据缺口、非代码缺口、非必须真机 Hook。
