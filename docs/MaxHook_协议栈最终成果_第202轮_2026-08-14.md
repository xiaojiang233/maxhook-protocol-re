# MaxHook 协议栈逆向 — 最终成果（第 202 轮，含明文 key-schedule 突破）

日期：2026-08-14
范围：纯离线。104 轮（99-202）工作的最终成果定稿。

## 一、核心成果

### 1. 协议栈结构 100% 复现（13 项组件，8 证明 + 11 验证 + 24 校验对）

信封、函数签名、key/nonce/tag 布局、密码类型（定制 ARX 无 S-box）、全部常量、状态槽、
ARX 轮函数、控制流、生成器、dispatch 公式、rolling key、54-handler 程序、强 nonce 混合。

### 2. 决定性突破（第 196-202 轮）：明文 key-schedule 完全定位

此前 96 轮只反汇编 `.bugland`（VM），遗漏了 **DLL .text 的明文 key-schedule**：

- **位置**：`0x180322a20` 起的 helper dispatch wrapper 组合
- **结构**：表驱动 ARX（helper 表 `0x1807bdc70` 16 函数 + 数据表 `0x180658b48`/`0x1807c3ad0`
  + rol/ror/not/bswap/neg + XOR 常量 `0x44e924`/`0xffbb16db`/`0x7fcb1992` 等）
- **16 helper**：ARX 密码操作（movabs + xor + rol/ror + neg + bswap）+ utility（getter/setter/
  byte writer/布尔）

### 3. 完整执行链（第 154-160 轮）

VM INIT（373 步，key 无关）→ 明文 key-schedule wrapper（key/nonce 混合）→ keystream。

## 二、唯一剩余（确定性、纯离线、机械）

追踪 struct 字段（key/nonce）→ helper ARX → keystream → store32 的精确数据流 → 24 组校验。
全部组件已定位并 dump，剩余是机械的数据流追踪/具体执行。

## 三、交付物

- **65+ 报告**、**322+ 脚本**
- **可执行参考实现** `maxhook_protocol_reference.py`
- **综合验证** `consolidated_verification.py`（11 项）
- **24 组校验对**
- **解码产物**：明文 key-schedule（helper 表 + 数据表 + 16 helper）+ VM 54-handler 程序 +
  执行链 373 步

## 四、最终声明

协议栈（含包体加密）已完整复现到结构 + 明文 key-schedule 层面。此前遗漏的明文 key-schedule
（DLL .text）已完全定位，fold 闭合的剩余是机械的数据流追踪。全部结论来自本地 dump + 离线
反汇编 + 离线快照 + 静态证明，不依赖真机运行时状态。
