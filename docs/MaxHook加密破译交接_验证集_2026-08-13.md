# MaxHook 外层加密 · 破译进展与验证集交接（2026-08-13 17:48）

> 给 VM 硬破 agent 的加密破译交接。核心：动态手段已穷尽，交给你一份**现成的 7 组验证集**用于验证你的 VM 解出结果。

## 一、结论摘要

外层上报信封（`POST security.mcbjd.net/api/v3/report`）的加密：
- **算法 = Themida VM 字节码化的私有流密码**，标准库（AES-GCM/CCM/CTR/OFB/CFB/ChaCha20/Salsa20/RC4）**全部排除**
- **key 材料、明文、输出信封已全部动态拿到**（7 组同会话对齐样本）
- 唯一能完全还原的路径 = **VM 字节码反编译**（你正在做）

## 二、加密函数硬坐标

```
明文 .text 加密边界:  0x180324610 = jmp 0x181523001     （外层信封加密）
VM 入口:              0x181523001 = VM 序言（保存寄存器）
VM dispatcher:        0x180c43fdd  （明文，boot/runtime 一致）
VM handler 表:        0x180c0c000-0x180c0d300（movabs+xor 常量解码 + jmp 回 dispatcher）
key 读取点:           0x180322e30 = movzx edx, [rdi]     （逐字节读 96 hex）
                      所属函数 0x180322d10（明文混淆，间接调用 [r12+rax*8]）
```

**参数布局（hook 验证 7/7）**：
```
RCX = output envelope（4 连续 std::string: kid+0x00 / nonce+0x20 / ciphertext+0x40 / tag+0x60）
RDX = input32 = KID（32 hex 字符）
R8  = input64 = 会话密钥材料（64 hex 字符 = 32 字节）
R9  = context（JSON 序列化字段表 @ rbp+0x38，含 "hpac.v3.session.report.req"）
[RSP+0x20] = plaintext JSON（std::string）
```

## 三、关键数据流发现（本轮）

1. **`0x180322e30` 读的 96 字节 = `kid(32hex) + input64(64hex)` 拼接**（12+ 次调用验证恒定）
2. `input32 == output kid`（恒等）
3. `input64` = 32 字节高熵会话密钥（hex 编码 64 字符），**无派生关系**（与 h2_cantor/device/session 均无哈希/XOR 关联）→ 极可能来自 `session/exchange` 端点协商
4. `0x180322d10` 函数逐字节处理这 96 hex（疑似 hex 解码或字符变换），是明文混淆函数（非 VM）

## 四、验证集（现成的 7 组样本）

文件：`E:\Coding\S1mple\target\crypto_verify_set.json`

```json
{"schema": "maxhook.crypto.verify-set/v1", "samples": [7 组]}
```

每组含：`kid`、`key_material_64hex`、`plaintext`（明文 JSON）、`nonce_24hex`（12B）、`ciphertext_hex`、`tag_32hex`（16B）、`output_kid`。

**同一次游戏会话**：key_material 恒定（`30bfeafea2cc438dfa757a8c4db06c9181ce50dc73fd453ca187c9e0e04b0345`），nonce/ciphertext/tag 每次变化，密文长度 = 明文长度（流式、无填充）。

**你解出算法/密钥后的验证方法**：用每组 `(key_material, nonce, plaintext)` 正向加密，比对 `ciphertext` + `tag`；7 组全部匹配即还原成功。

### 关键材料（本会话）

```
key_material (input64, 32B): 30bfeafea2cc438dfa757a8c4db06c9181ce50dc73fd453ca187c9e0e04b0345
kid (16B):                   00737515f460e84f3c1ec942f2b83960
device_id (8B):              E43A2C0F779F0DF2
session_id (16B):            2933FD480B8E9D03520ABC3438F29CD9
h2_cantor:                   v2:92531770bb30b00ce52bd3260df1a2da:3dcdf82480cb7be3dc507cc9b729acc02bd83c64325098a3d308a385886a3f24
protocol tag (context):      hpac.v3.session.report.req
```

## 五、算法排除清单（不用重复踩）

已穷举验证（全部 0 命中）：
- **算法**：AES-GCM、AES-CCM、AES-CTR（nonce 12B+counter 4B BE/LE、8B+8B）、AES-OFB、AES-CFB、ChaCha20（12B/8B nonce）、Salsa20、RC4、HMAC-SHA256/SHA1/MD5 截断 tag
- **key 派生**：input64 的 hex 解码/ASCII/哈希/字节序(rev4/rev8)/XOR 掩码，及与 kid/h2_cantor(MD5+SHA256)/device/session 的全部 SHA256/HMAC 组合（60+ 项）
- **静态特征**：`.bugland` 无任何标准密码常量（AES S-box/T-tables/ChaCha `expand 32-byte k`/SHA256 K/H/MD5/AES-NI 全零命中）

## 六、对 VM 硬破的具体建议

1. **优先攻加密函数 VM**（`0x181523001 → dispatcher 0x180c43fdd`），它独立于主入口 VM
2. 目标一：**key 派生**——VM 内如何把 `input64(32B)` + `kid(16B)` 变成实际加密 key
3. 目标二：**流密码本体**——handler 组合里的轮函数（ARX 特征，无查表）
4. 注意：`0x180322d10` 是明文混淆函数（hex 解码/变换），可先静态解它理解 96 hex 的预处理
5. **用第四节验证集验证**——解出即验，不用再抓游戏

## 七、工程方案 1（备选，不依赖破 VM）

`capture_maxhook_encrypt_boundary.py/.js` 已在加密边界 hook 截获 (plaintext → envelope) 完整映射，proxy 可复用真实加密输出——若 VM 反编译耗时过长，这是可立即落地的替代。

---

*敏感材料仅存本地，未上传未公开。*
